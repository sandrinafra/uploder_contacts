<p id="notice"><%= notice %></p>

<p>
  <strong>File:</strong>
  <% @import.contacts.each do |ct| %>
      <p><%= "#{ct.firstname} #{ct.lastname} #{ct.email}" %></p>
  <% end %>
</p>

<%= link_to 'Edit', edit_import_path(@import) %> |
<%= link_to 'Back', imports_path %>
